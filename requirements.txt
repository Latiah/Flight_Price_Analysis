# Installed into the Airflow image by the Dockerfile, resolved against the
# official Airflow 2.10.5 / Python 3.11 constraints file. Airflow itself is not
# listed here — it comes from the `apache/airflow:2.10.5-python3.11` base image,
# so the version is pinned in exactly one place.

# Airflow providers (MySQL staging + PostgreSQL analytics hooks)
apache-airflow-providers-mysql>=5.5.0
apache-airflow-providers-postgres>=5.10.0

# Data processing
pandas>=2.1.0
# SQLAlchemy is deliberately unpinned: Airflow 2.10 requires SQLAlchemy 1.4.x and
# the constraints file pins 1.4.54, so asking for >=2.0 here can never resolve.
# The version Airflow itself depends on is the one to use.
SQLAlchemy
PyMySQL>=1.1.0
psycopg2-binary>=2.9.9

# Configuration
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
